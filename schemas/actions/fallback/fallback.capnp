@0xc18dfbdef887c496;

# Tick children left-to-right; succeed if ANY child succeeds.
# Children are carried by ActionSpec.children; this struct holds
# Fallback-specific configuration (extend with new fields as needed).

struct Fallback {
  # Reserved for future per-fallback configuration.
}
