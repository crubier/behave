@0xf5d780766c879f63;

# Tick children left-to-right; succeed only if ALL children succeed.
# Children are carried by ActionSpec.children; this struct holds
# Sequence-specific configuration (extend with new fields as needed).

struct Sequence {
  # Reserved for future per-sequence configuration.
}
