# Topic: SimPose

Camera pose commands for the Unreal Engine simulator. Published by `send_pose`, consumed by the `sim_bridge` inside UE5.

## Topic

- **`behave/SimPose`** -- 256-byte buffer (small fixed-size poses)
