# Topic: Behave Request

ActionArgs behavior trees sent from Communicate to Behave. Topic name: `behave/ActionRequest`.

Raw `ActionArgs` schema messages -- no wrapper. The schema is defined in `actions/action.schema`.
