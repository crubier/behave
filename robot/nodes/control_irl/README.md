# Node: Control IRL

Real hardware flight controller interface. Will eventually communicate with actual flight controller hardware (e.g., MAVLink, UART, CAN bus).

Currently a stub awaiting hardware integration.

## Topics

- Subscribes to: `behave/ControlRequest`
- Publishes to: `behave/ControlResponse`, `behave/ControlStatus`
