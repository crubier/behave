# Node: Sense Metaverse

Sensor data from the UE5 metaverse simulation. Subscribes to `behave/SimStatus` and publishes the pose as `behave/SenseStatus`.

## Topics

- Subscribes to: `behave/SimStatus`
- Publishes to: `behave/SenseStatus`
