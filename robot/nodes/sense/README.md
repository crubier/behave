# Node: Sense

Sensor data acquisition node. Will eventually publish sensor state (IMU, GPS, battery, etc.) over iceoryx2 for other nodes to consume.

Currently a stub that emits periodic heartbeats.
