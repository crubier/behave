name: mock
description: >
  Lightweight testing mode with a dumb mock control node
  and a mock GCS that sends a sample mission automatically.
nodes:
  sense-mock:
  control-mock:
  behave:
  communicate:
  gcs-mock:
