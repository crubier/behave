# Action: Fallback

Composite action that ticks children left-to-right. Succeeds as soon as **any** child succeeds. Continues to the next child when one fails. Fails only if all children fail.

Also known as "Selector" in some behavior tree implementations.
