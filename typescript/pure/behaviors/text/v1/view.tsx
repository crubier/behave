import * as React from "react";
import { TextArgument, TextResult } from "./types"

export function TextArgumentView(props: { argument: TextArgument }) {
  return <div>Ok</div>
}

export function TextResultView(props: { result: TextResult }) {
  return <div>Ok</div>
}
