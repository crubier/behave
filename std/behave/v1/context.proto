syntax = "proto3";

package behave.v1;

import "behave/v1/common.proto";

message Context {
  Pose start_pose = 1;
  Pose return_pose = 2;
  repeated Geofence geofences = 3;
  GlobalGraph global_graph = 4;
  string vehicle_model = 5;
}
